focus_tree = {
	id = W41_focus
	
	country = {
		factor = 0
		
		modifier = {
			add = 10
			tag = W41
		}
	}
	
	default = no
	
	shared_focus = army_effort
	shared_focus = aviation_effort
	shared_focus = naval_effort
	shared_focus = industrial_effort
	shared_focus = political_effort

	focus = {
		id = integrate_satelliteW41AST
		icon = GFX_focus_eng_move_to_secure_the_dominions
		text = integrate_satelliteAST
		bypass = {
			AST = {
				OR = {
					NOT = { is_subject_of = ROOT }
					has_war_with = ROOT
				}
			}
		}
		x = 24
		y = 0
		cost = 10
		available = {

		}
		select_effect = {
			AST = {
				every_owned_state = { set_state_flag = is_being_integrated }
			}
		}
		completion_reward = {
			annex_country = { target = AST transfer_troops = yes }
			every_owned_state = {
				limit = { has_state_flag = is_being_integrated }
				add_core_of = ROOT
				clr_state_flag = is_being_integrated
			}
		}
		ai_will_do = {
			factor = 10
		}
	}

	focus = {
		id = integrate_satelliteW41SAM
		icon = GFX_focus_eng_move_to_secure_the_dominions
		text = integrate_satelliteSAM
		bypass = {
			SAM = {
				OR = {
					NOT = { is_subject_of = ROOT }
					has_war_with = ROOT
				}
			}
		}
		x = 24
		y = 1
		cost = 10
		available = {

		}
		select_effect = {
			SAM = {
				every_owned_state = { set_state_flag = is_being_integrated }
			}
		}
		completion_reward = {
			annex_country = { target = SAM transfer_troops = yes }
			every_owned_state = {
				limit = { has_state_flag = is_being_integrated }
				add_core_of = ROOT
				clr_state_flag = is_being_integrated
			}
		}
		ai_will_do = {
			factor = 10
		}
	}

	focus = {
		id = integrate_satelliteW41W08
		icon = GFX_focus_eng_move_to_secure_the_dominions
		text = integrate_satelliteW08
		bypass = {
			W08 = {
				OR = {
					NOT = { is_subject_of = ROOT }
					has_war_with = ROOT
				}
			}
		}
		x = 24
		y = 2
		cost = 10
		available = {

		}
		select_effect = {
			W08 = {
				every_owned_state = { set_state_flag = is_being_integrated }
			}
		}
		completion_reward = {
			annex_country = { target = W08 transfer_troops = yes }
			every_owned_state = {
				limit = { has_state_flag = is_being_integrated }
				add_core_of = ROOT
				clr_state_flag = is_being_integrated
			}
		}
		ai_will_do = {
			factor = 10
		}
	}

}